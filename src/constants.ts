// Standalone constants so index ↔ generator don't form an import cycle (the same
// TDZ trap create-packkit-py's constants.ts avoids).
export const GENERATOR_ID = 'go';
export const PROVENANCE_SCHEMA_VERSION = 1;
